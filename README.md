# Weather

### Setup
##### Cloning the project
`git clone https://github.com/sudheeshshetty/Weather-App.git`

##### Backend - Node
* Install the node modules.
* Run `npm install`
* Run `npm start`

##### Frontend - Angular2
